detail changes:

- skip user selection during the authentication if there is only one user with a password
- added FallbackHome to the whitelist
- refactored text which is shown when a authentication is required
- updated build tools and libraries
